; height
; level n
; count-leaves
; remove-leaves
; map-tree

;;? binary-heap?

; fold-tree

; invert

; balanced

; bst?
; insert-bst

; max-path

;;;;; Associative lists
; make-alist f keys

; keys
; values
; assoc, assq, assv
; del-assoc
; acc-assoc

; search

;;;;
; index-array
; histogram
; group-by
;? unique
;;;;; Graphs

;? add-edge
;? add-vertex
; vertices
; children v g
; edge?
; map-children
; search-child

; childless
; parents
; symmetric

; dfs
; bfs
; 

; indegree
; outdegree

; graph-transpose



;;;;; Streams
; delay
; force

; the-empty-stream
; cons-stream
; head
; tail
; empty-stream?

;? примери за бездънни рекурсии
; take, drop, stream-ref

; map-stream
; filter-stream
; zip-streams

; ones
; nats
; from

; fibs
; primes


; repeat-list - безкраен поток от повтарянето на списък
